/** 3D garment studio (same app as garment-3d, fast path at /studio/). */
export const STUDIO_3D_PATH = "/studio/";

/** Direct embed path for iframe / deep links. */
export const STUDIO_3D_APP_PATH = "/garment-3d/";

/** Primary 2D AI try-on (ladies). */
export const TRYON_2D_LADIES_PATH = "/ladies_try_on/";

/** Men's 2D try-on. */
export const TRYON_2D_MEN_PATH = "/";
