# Archived Vercel Python handlers (not deployed)

Try-on API runs on **Render** via `replicate_tryon_server.py` at repo root.

These files were removed from `api/` so Vercel does not bundle Python (~2GB Lambda error).

Production:
- API: https://threed-studio-deploymentt.onrender.com
- Frontend: Vercel with `VITE_TRYON_BACKEND` set to Render URL
